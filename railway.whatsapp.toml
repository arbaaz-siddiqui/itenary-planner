[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=/app uvicorn surfaces.whatsapp_app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
