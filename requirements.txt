# EXACT pins frozen from the local venv so the Railway deployment installs the
# identical packages the app is developed and verified against. Loose >= pins
# let Railway resolve newer versions with different behaviour/DOM (e.g. the
# floating-navbar CSS silently stopped matching, and LangChain/LangGraph 0.x vs
# 1.x differ at the API level). Bump deliberately and re-verify after upgrading.
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
structlog==25.5.0
requests==2.34.2
langchain==1.3.2
langchain-core==1.4.0
langchain-anthropic==1.4.4
langchain-openai==1.2.2
langgraph==1.2.2
langgraph-checkpoint-sqlite==3.1.0
anthropic==0.105.2
mcp==1.27.1
fastmcp==3.3.1
streamlit==1.58.0
fastapi==0.136.3
uvicorn[standard]==0.48.0
twilio==9.10.9
pandas==3.0.3
openpyxl==3.1.5
filelock==3.29.0
fpdf2==2.8.7
# LiveKit SERVER SDK only (lightweight: protocol + aiohttp, NO agents/onnx/av).
# The chat service needs this so the "Get a call" button can dispatch an outbound
# call via livekit_config.place_call() without the heavy voice-worker stack.
livekit-api==1.2.0
