pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0
structlog>=24.1
requests>=2.32
langchain>=0.3
langchain-core>=0.3
langchain-anthropic>=0.2
langchain-openai>=0.2
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
anthropic>=0.34
mcp>=1.0
fastmcp>=0.4
# Pinned: the nav-pill CSS in surfaces/ui_theme.py targets Streamlit's tab DOM
# (div[data-baseweb="tab-list"]). Newer Streamlit changes that structure, so the
# floating navbar silently stops rendering. Pin to the version we build the UI
# against. Bump deliberately + re-verify the navbar when upgrading.
streamlit==1.58.0
fastapi>=0.115
uvicorn[standard]>=0.30
twilio>=9.3
pandas>=2.2
openpyxl>=3.1
filelock>=3.15
fpdf2>=2.7
